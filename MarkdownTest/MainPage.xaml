<Page x:Class="MarkdownTest.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MarkdownTest"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <local:MarkedControl  x:Name="markCtrl1" MarkdownText="# Here is a cheat sheet: B"  />
        <Button x:Name="button" Grid.Column="1" Content="LOAD" Click="button_Click"/>
        <!--<ScrollViewer>-->
        <!--
        <local:MarkedControl  x:Name="markCtrl1"  MarkdownText="# Here is a cheat sheet: B" />
        <local:MarkedControl x:Name="markedCtrl" MarkedReady="markedCtrl_MarkedReady"/>
        -->
        <!--
        <StackPanel >
        </StackPanel>
        -->
        <!--</ScrollViewer>-->
        <!--
        <local:MarkedControl Grid.Column="1" MarkdownText="# Here is a cheat sheet:C" />
        -->
        <!--
        <local:AltJavaScriptControl
            Grid.Column="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            />
        <Grid Grid.Row="1">
            <local:PdfControl HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </Grid>
        <local:JsDebug
            Grid.Column="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ></local:JsDebug>
        -->
    </Grid>
</Page>
